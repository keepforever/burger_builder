import React from 'react';

const burgerIngredient = ( props ) => {

};

export default burgerINgredient;

// we use the curly braces in this stateless functional component because
// we want to run some logic before rendering JSX.
